apiVersion: v1
kind: Service
metadata:
  name: nginx-app
  namespace: mytenant
  labels:
    app: nginx-app
spec:
  selector:
    app: nginx-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
