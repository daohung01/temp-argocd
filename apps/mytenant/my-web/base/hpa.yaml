apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: my-web
  namespace: mytenant
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: my-web
  minReplicas: 1
  maxReplicas: 3
