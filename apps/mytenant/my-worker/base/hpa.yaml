apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: my-worker
  namespace: mytenant
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: my-worker
  minReplicas: 1
  maxReplicas: 3
